- name: slack_statusbots
  platform: slack
  api_key: !secret slack_api_key
  default_channel: '#statusbots'

- name: slack_home_automation
  platform: slack
  api_key: !secret slack_api_key
  default_channel: '#home-automation'

- name: slack_bot_chat
  platform: slack
  api_key: !secret slack_api_key
  default_channel: '#bot_chat'

- name: slack_shopping_list
  platform: slack
  api_key: !secret slack_api_key
  default_channel: '#shopping-list'

- name: camera_bot
  platform: twitter
  consumer_key: !secret twitter_consumer_key
  consumer_secret: !secret twitter_consumer_secret
  access_token: !secret twitter_access_token
  access_token_secret: !secret twitter_access_token_secret

- name: i3_twitter
  platform: twitter
  consumer_key: !secret i3_twitter_consumer_key
  consumer_secret: !secret i3_twitter_consumer_secret
  access_token: !secret i3_twitter_access_token
  access_token_secret: !secret i3_twitter_access_token_secret
